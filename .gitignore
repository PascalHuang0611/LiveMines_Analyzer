LiveMines_bill_records_20260630_143348*
LiveMines 遊戲數學原理與規則解析 V12_6*
*.csv
Old_Backup/

# 玩家暱稱對照表 —— 內含真實玩家的帳號與暱稱，本 repo 是公開的，不要提交。
# 工具改從選取的資料夾直接讀這個檔，不需要上傳到任何地方。
nicknames.json

# 平台匯出的報表。實測 query_*.xlsx 內含明文 login_name 與 customer_id，
# 跟 nicknames.json 是同一類東西（可還原到真實玩家），一樣不能進公開 repo。
*.xlsx
*.xls

# 圖片預設不提交（避免誤傳截圖、含真人照片的素材）。
# 只有 cover*.png 是刻意要隨程式發布的封面圖，需確認不含可辨識的真人。
*.png
!cover*.png

# 遊戲數學：模擬器原始碼、設定檔（完整賠率表、RTP 目標、風控參數）、模擬報告。
# 本 repo 是公開的 —— 這些一旦外流，任何人都能反推這個上線中遊戲的經濟結構。
# analyzer 只在程式碼註解裡引用推導出來的規則，不需要這些檔案就能跑。
理論/
*_Config.json
*_Config_Report.txt
LiveMines_Simulator*
